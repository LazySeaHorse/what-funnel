# adapters/matrix-mautrix — Matrix/mautrix Channel Adapter

> **Stub** — built in Build Prompt 2 (Channel Ingestion).

Go. Implements the `ChannelAdapter` interface (defined in `packages/go-common`).
Bridges mautrix events (WhatsApp, Instagram, Messenger, Telegram) into normalized `InboundEvent` structs published to Redis Streams.
